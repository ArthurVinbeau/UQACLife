<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlack</item>
        <item name="android:textColor">@color/colorGris</item>

</style>
    <style name="SwichStyle" parent="AppTheme">
        <item name="android:textColor">@color/colorGris</item>
    </style>

    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <!-- Optional, on Android 5+ you can modify the colorPrimaryDark color to match the windowBackground color for further branding-->
        <!-- <item name="colorPrimaryDark">@android:color/white</item> -->
    </style>

    <style name="couleur_monday" parent="Theme.AppCompat.Light.NoActionBar">
        <!--<color name="colorOrange1">#FFC107</color>
        <color name="colorOrange2">#FF9B00</color>-->
    </style>

</resources>
